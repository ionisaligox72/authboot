{
  "name": "authboot",
  "version": "1.0.0",
  "description": "a simple slay preboot to instantiate an auth middleware and a custom/default lookup function on the app object",
  "main": "index.js",
  "scripts": {
    "test": "nyc mocha",
    "eslint": "eslint-godaddy --fix -c .eslintrc ./*.js",
    "pretest": "npm run eslint",
    "test-travis": "nyc --reporter=lcov mocha"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:warehouseai/authboot.git"
  },
  "keywords": [
    "slay",
    "preboot",
    "express",
    "http"
  ],
  "author": "GoDaddy Operating Company LLC",
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^1.0.3",
    "diagnostics": "^1.1.0",
    "express-basic-auth-safe": "^1.1.4"
  },
  "devDependencies": {
    "assume": "^1.5.2",
    "eslint": "^4.18.2",
    "eslint-config-godaddy": "^2.1.0",
    "eslint-plugin-json": "^1.2.0",
    "eslint-plugin-mocha": "^4.12.1",
    "mocha": "^5.0.4",
    "nconf": "^0.10.0",
    "nyc": "^11.6.0"
  }
}
